# 智能宠物屋系统 (Esp32PetHome) Agent 开发协作规范

所有负责本项目的 AI Agent，在开发、调试、文档编写等环节，必须绝对遵守以下规范（规矩）。这是该项目顺畅交付的红线。

## 1. 语言与注释规范
- **客观的中文注释**：代码注释和文档编写必须采用全中文，并且呈现工程化的客观描述。严禁在注释或文档中出现“帮你”、“你说的”、“本人”等主观人称词汇。
- **文件编码限制**：所有代码、文档及配置文件，统一且只能使用 **UTF-8** 编码。

## 2. 行为与执行规范
- **绝对依从 PRD**：必须 100% 根据 `docs/PRD.MD` 的内容完整执行开发任务。PRD 中所列功能和简化点不折不扣地全集实现，绝不允许遗漏或擅自篡改业务逻辑。
- **应用侧 SDK 版本锁定**：华为应用侧 (鸿蒙 App) 的开发代码，严格基于 **API 17** 规范与 SDK 进行编写，切勿乱用错误或废弃版本的 API。

## 3. 工程核心目录划分
项目采取严格的端-应用分离存放：
- **应用侧（鸿蒙应用）**：`PetHome_APP` 
- **设备侧（ESP32固件）**：`PetHome_Device`

## 4. 专项文档 (docs 目录) 作用与维护规矩

所有专项文档位于 `docs/` 目录，是工程和论文的基石，须按以下约束严格编写和维护：

1. **`HuaweiCloud.MD`（云端参数与自动化环境指导）**
   - 记录代码中所需的所有华为云平台侧鉴权参数（包括 AK/SK、设备密钥、三元组等）。这属于个人课设演示项目，**不考虑任何安全性，上述参数一律明文存放在此文档且直接硬编码在业务代码中**。
   - **云能力自动化建设**：若发现缺失项目所需的产品或设备实例，AI 使用 `chrome devtools mcp` 控制浏览器自动获取参数。获取过程如需登录，仅需提示用户扫码或输入密码登录，随后的创建设备、解析物模型、拿到鉴权参数等流程，**必须由 AI 严谨且全自动地闭环搞定，绝不允许让用户参与繁琐的云端操作**。

2. **`Interface.MD`（接口协议定义）**
   - 作为前后端或端云的接口文档，完整记录系统应用、云端与设备之间的所有交互接口（明确 MQTT Topic、下发命令、上传数据 Payload 与格式）。

3. **`Wiring_Guide.MD`（硬件选购与接线明细）**
   - **精简选购**：明确写明用户需要购买的元器件列表与精确型号，要求淘宝包买得到，**越精简越好，剔除所有非必要外设**。
   - **接线指导**：必须根据项目中设备端代码里的 GPIO 映射定义来写，针脚对针脚讲得清清楚楚，绝对不能遗漏，确保小白也能照接不误（不烧板子）。

4. **`TECH.MD`（技术体系与架构约束）**
   - 描述本项目的开发环境、工程结构与相关的关键代码约定。必须在写任何一行正式业务代码前建立并确认此技术口径。

5. **`Acceptance_Checklist.MD`（真人验收防呆清单）**
   - 编写供真人答辩走查的验收测试用例。验收不仅要求端到端可验证，其中的流程操作和观测现象还需要可以直观截图，以便直接用于撰写课设结题论文。

## 5. Superpowers 技能架构与代码门禁
本项目遵循 Superpowers 工程和测试规范要求：
- **体积死线约束**：单一文件内代码量**严禁超过 1000 行**，单函数（方法）体积**严禁超过 200 行**。
- **重构原则**：若超限，必须通过严谨的架构设计拆分模块，模块切分需职能分明、接口互不耦合。**绝不允许通过删掉健壮性代码或容错逻辑的旁门左道来蒙混缩减行数**。
- **门禁脚本自动化**：构建开发时，需单独搭建一个用于代码统计和合规审计的“门禁脚本”。在每次代码阶段编写结束后，必须跑一次门禁脚本验证是否双双超限，以免代码变成一坨雪球。

## 6. Plan 文件命名规范
- `docs/Plan` 下后续文件统一命名为：`年月日时分_任务简短标题.md`。
- 示例：`202602210012_PRD剩余任务补齐.md`。
