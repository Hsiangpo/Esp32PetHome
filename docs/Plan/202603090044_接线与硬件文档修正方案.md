# 接线与硬件文档修正实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按 `PetHome_Device` 当前代码真实行为，统一修正文档中的硬件选型、采购清单、接线说明、验收口径与接口描述。

**Architecture:** 以 `PetHome_Device/include/board_pins.h`、`PetHome_Device/src/sensor_manager.cpp`、`PetHome_Device/src/actuator_manager.cpp`、`PetHome_Device/src/button_manager.cpp`、`PetHome_Device/src/display_manager.cpp`、`PetHome_Device/src/main.cpp` 为唯一真值来源；`docs/Wiring_Guide.MD` 负责购买与接线落地，`docs/PRD.MD` 负责需求口径，`docs/TECH.MD` 负责技术约束，`docs/Acceptance_Checklist.MD` 负责验收口径，`docs/Interface.MD` 负责端云命令与状态协议。

**Tech Stack:** Markdown、PlatformIO Arduino 固件、华为云 IoTDA MQTT / REST、HarmonyOS API 17。

---

### Task 1: 锁定代码真值

**Files:**
- Read: `PetHome_Device/include/board_pins.h`
- Read: `PetHome_Device/src/sensor_manager.cpp`
- Read: `PetHome_Device/src/actuator_manager.cpp`
- Read: `PetHome_Device/src/button_manager.cpp`
- Read: `PetHome_Device/src/display_manager.cpp`
- Read: `PetHome_Device/src/main.cpp`

**Step 1:** 提取全部 GPIO、输入输出逻辑、电平语义、初始化依赖。

**Step 2:** 标记所有与现文档不一致的硬件事实，尤其是 `GPIO4` 投喂输出、继电器触发电平、LED 直驱、液位输入电平、I2C 总线约束。

### Task 2: 重写硬件选购与接线文档

**Files:**
- Modify: `docs/Wiring_Guide.MD`

**Step 1:** 重写采购清单，拆成“按现代码可直接联调的最小清单”与“标准舵机实体化前置条件”。

**Step 2:** 写出逐模块针脚对针脚接线表，覆盖 VCC / GND / SDA / SCL / DT / SCK / OUT / IN / COM / NO。

**Step 3:** 补充供电拓扑、电平兼容、I2C 上拉、继电器触发类型、HX711 去皮、液位输出限压等防呆说明。

### Task 3: 同步需求与技术口径

**Files:**
- Modify: `docs/PRD.MD`
- Modify: `docs/TECH.MD`

**Step 1:** 将 PRD 中的执行器选型、供电、投喂实现说明与代码现状对齐。

**Step 2:** 在 TECH 中补充板级硬件口径与文档真值来源，明确 `GPIO4` 当前不是标准 RC 舵机 PWM 输出。

### Task 4: 同步验收与接口口径

**Files:**
- Modify: `docs/Acceptance_Checklist.MD`
- Modify: `docs/Interface.MD`

**Step 1:** 更新验收项对投喂执行器、继电器、LED 直驱的表述，避免与现固件行为冲突。

**Step 2:** 修正接口文档中的控制命令与响应示例，使其与当前命令处理代码一致。

### Task 5: 复核一致性

**Files:**
- Verify: `docs/Wiring_Guide.MD`
- Verify: `docs/PRD.MD`
- Verify: `docs/TECH.MD`
- Verify: `docs/Acceptance_Checklist.MD`
- Verify: `docs/Interface.MD`

**Step 1:** 逐文档核对关键术语：`GPIO4`、`feed_once`、`feed`、`高电平触发继电器`、`GPIO直驱 LED`、`LOW=低水位`。

**Step 2:** 确认修改均为 UTF-8 中文工程描述，且不引入与代码相矛盾的新口径。
